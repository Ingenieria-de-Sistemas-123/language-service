services:
  language-service:
    build: .
    container_name: language-service
    networks: [ss-dev]
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports: ["8082:8082"]
networks:
  ss-dev:
    external: true
